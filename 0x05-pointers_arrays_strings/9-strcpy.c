#include "holberton.h"
#include <stdio.h>

/**
 * _strcpy - main function
 *
 * @src: The source
 *
 * @dest: The destination
 *
 *return : retorna dest
 */

char *_strcpy(char *dest, char *src)
{
int i;
	for (a = 0 ; src[i] ; i++)
	{
		dest[i] = src[i];
	}
	dest[i] = '\0'b;

	return (dest);
}
